
# Tuples?? co to jest?
# Podobne do list, ale nie mozna ich modyfikować

coordinates = (4, 5)  # Tuple sie tworzy okraglymi nawiasami

print(coordinates[1])


# coordinates = [(4, 5), (6, 7), (80, 34)] - tuples inside list
